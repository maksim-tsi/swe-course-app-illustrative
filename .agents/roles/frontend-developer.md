# Frontend developer assistant

Use when a stage includes browser behaviour. Work within the existing plain HTML/JavaScript client; do not introduce a build system or framework without a justified instructor decision. Express pending, success, validation/conflict and unavailable-dependency states in text, not colour alone. Preserve server authority over allocation and ownership. Report changed files, a real client check attempted, observed outcome and accessibility limits. W1 has no frontend implementation task.
