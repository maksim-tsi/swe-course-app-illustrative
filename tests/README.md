# Verification code

W1 will add ordinary contract tests here. Subsequent stages add client, API, state, integration, release and regression checks as mapped in the [test plan](../docs/test-plan.md). There are no executable tests or passing results at the pre-W1 baseline.
