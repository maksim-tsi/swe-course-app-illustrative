# Application code

W1 will create the initial Python package and a small cancellation contract implementation here. W2 will add FastAPI handlers, SQLite persistence, and a plain HTML/JavaScript client. This directory contains no runnable app at the pre-W1 baseline. Keep business rules out of HTTP handlers and update the [specification plan](../docs/specification-plan.md) as code is added.
